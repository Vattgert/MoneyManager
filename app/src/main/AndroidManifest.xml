<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productmanagment">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".categories.CategoryActivity" />
        <activity android:name=".expenses.ExpensesActivity" />
        <activity android:name=".addexpenses.AddExpenseActivity" />
        <activity android:name=".expensedetailandedit.ExpenseDetailAndEditActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_place_key" />

        <activity android:name=".plannedpayment.PlannedPaymentActivity" />
        <activity android:name=".addplannedpayment.AddPlannedPaymentActivity" />
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".diagrams.DiagramActivity" />
        <activity
            android:name=".debts.DebtsActivity"
            android:label="@string/debts" />
        <activity
            android:name=".adddebts.AddDebtActivity"
            android:label="@string/debt_creating" />
        <activity
            android:name=".debtsdetailandedit.DebtDetailAndEditActivity"
            android:label="@string/debt_edit" />
        <activity
            android:name=".debtpayments.DebtPaymentsActivity"
            android:label="@string/debt_payments" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".authorization.AuthorizationActivity"></activity>
        <activity android:name=".userinfo.UserInfoActivity" />
        <activity android:name=".addgroup.AddGroupActivity" />
        <activity android:name=".groupeditanddetail.GroupDetailAndEditActivity" />
        <activity android:name=".userrights.UserRightsActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".addaccount.AddAccountActivity" />
        <activity android:name=".accountdetailandedit.AccountDetailAndEditActivity" />
        <activity android:name=".addgoal.AddGoalActivity" />
        <activity android:name=".addcurrency.AddCurrencyActivity" />
        <activity android:name=".basecurrency.BaseCurrencyActivity" />
        <activity android:name=".currencydetailandedit.CurrencyDetailAndEditActivity" />
        <activity android:name=".goaldetailandedit.GoalDetailAndEditActivity" />
        <activity android:name=".goalDetail.GoalDetailActivity" />
        <activity android:name=".goalEdit.GoalEditActivity" />
        <activity android:name=".categoryexpenses.CategoryExpensesActivity"></activity>
    </application>

</manifest>